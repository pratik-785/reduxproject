import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: { width: '100%',marginVertical:6 },
    btn: { borderRadius: 4, width: 180, height: 30, justifyContent: 'center', alignItems: 'center', backgroundColor: 'red' },
    btnTxt: { fontSize: 20, fontWeight: '600', color: '#fff' }
})

export default styles;
